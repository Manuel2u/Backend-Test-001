
import { addMethod, Schema } from 'yup';

// addMethod(Schema, 'ObjectId', function (message = 'Invalid ObjectId') {
//     return this.test('ObjectId', message, value => value == null || ObjectId.isValid(value as string));
// }); 